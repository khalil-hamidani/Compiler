LANGAGE Mini_Algo
VAR

    hight , a, b, c, d : INT;  
    {a : FLOAT;}
    isColored : BOOL; 
    angle = -2 : CONST INT;    
    test = 1 : CONST FLOAT;    {CONST NON DECLARE}
    max : INT;

    INT Function Surface
    VAR
        {max : INT;}
        pi = 3.14 : CONST FLOAT; 
    BEGIN
        max = max * max;
        Return max;
    END

BEGIN 

    {cmnt
    cmnt
    cmnt}
    max = 5 / 5;  

    While (max > 2)
    BEGIN
      max = 5 / 1;
    END

    If (max == 2)
    BEGIN
        max = 5 / 5;
        max = -5;
    END
    
END 